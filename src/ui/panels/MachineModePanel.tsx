/**
 * Print / CNC mod secimi ve CNC'ye ozel girdiler (Faz 6):
 * ham malzeme X/Y/Z, takim tipi ve capi, voxel cozunurlugu.
 * TODO(sonnet).
 */
export function MachineModePanel() {
  return <div className="panel panel--machine-mode" data-placeholder="MachineMode: TODO" />;
}
