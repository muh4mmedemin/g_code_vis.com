/**
 * Ust serit: dosya ac / ornek dosya yukle, Print-CNC mod secici,
 * kamera preset'leri, tema.
 * TODO(sonnet).
 */
export function Toolbar() {
  return <header className="toolbar">G-code Visualizer</header>;
}
