/**
 * Simulasyon kontrolleri (Faz 3): play/pause, adim ileri/geri, hiz secici,
 * timeline scrubber (hareket indeksi veya sure uzerinden).
 * TODO(sonnet).
 */
export function PlaybackControls() {
  return <div className="panel panel--playback" data-placeholder="PlaybackControls: TODO" />;
}
