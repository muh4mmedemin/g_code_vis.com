/**
 * Gorunum anahtarlari: travel goster/gizle, renk modu (tip/katman/feedrate),
 * grid, calisma hacmi, eksenler.
 * TODO(sonnet).
 */
export function ViewOptionsPanel() {
  return <div className="panel panel--view-options" data-placeholder="ViewOptions: TODO" />;
}
