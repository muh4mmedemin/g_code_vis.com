/**
 * Dikey katman slider'i (Faz 2): "1..N katmani goster" + izole mod anahtari.
 * Aktif katmanin Z degerini yaninda gosterir.
 * TODO(sonnet).
 */
export function LayerSlider() {
  return <div className="panel panel--layer-slider" data-placeholder="LayerSlider: TODO" />;
}
