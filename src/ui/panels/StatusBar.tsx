/**
 * Alt serit: parse durumu/ilerlemesi, satir sayisi, aktif katman ve Z,
 * hareket imleci, uyari sayisi.
 * TODO(sonnet).
 */
export function StatusBar() {
  return <footer className="statusbar" />;
}
