/** Editor degisiminde reparse'i geciktirmek icin. TODO(sonnet). */
export function debounce<T extends (...args: never[]) => void>(_fn: T, _ms: number): T {
  throw new Error('NOT_IMPLEMENTED: debounce');
}
