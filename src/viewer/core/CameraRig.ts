/**
 * OrbitControls sarmalayicisi (orbit / pan / zoom) + kamera preset'leri.
 * SceneManager tarafindan kullanilir; disariya sizmaz.
 * TODO(sonnet): three/examples/jsm/controls/OrbitControls entegrasyonu.
 */
export class CameraRig {
  dispose(): void {
    throw new Error('NOT_IMPLEMENTED: CameraRig.dispose');
  }
}
