/**
 * CodeMirror icin minimal G-code dil tanimi (StreamLanguage yeterli).
 * Token siniflari: komut (G/M), parametre harfi, sayi, yorum, satir numarasi.
 * TODO(sonnet).
 */
export function gcodeLanguage() {
  throw new Error('NOT_IMPLEMENTED: gcodeLanguage');
}
